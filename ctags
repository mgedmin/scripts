#!/bin/sh
if [ -d .git ] && ! grep -q '^tags$' .git/info/exclude; then
    echo tags >> .git/info/exclude
fi
if [ -x ~/src/ctags/ctags ]; then
    exec ~/src/ctags/ctags "$@"
else
    exec /usr/bin/ctags "$@"
fi
